[project]
name = "spruce"
version = "0.6.1"
description = "Spruce - Solving Phases of the Rubik's cube."
authors = [
    { name = "Tufte, Martin Gudahl" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.13, <3.14"
dependencies = [
    "numpy==2.1.1",
    "matplotlib==3.9.2",
    "pre-commit==3.8.0",
    "black==24.8.0",
    "attrs>=23.2.0",
    "typer>=0.12.5",
    "bidict>=0.23.1",
    "python-json-logger>=2.0.7",
    "ruff>=0.7.3",
    "ty>=0.0.1a23",
    "pytest>=8.3.2",
    "cattrs>=26.1.0",
    "pytest-cov>=7.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest==8.3.2",
]

[dependency-groups]
dev = [
    "types-requests>=2.32.4.20250913",
]
app = [
    "streamlit>=1.55.0",
    "extra-streamlit-components==0.1.81",
    "st-annotated-text>=4.0.2",
]
experiments = [
    "tqdm>=4.67.1",
    "types-tqdm>=4.67.0.20250809",
    "pyvis>=0.3.2",
]


[tool.black]
line-length = 100
preview = true

[tool.ruff]
line-length = 100

[tool.ruff.lint]
extend-select = ["I", "SIM", "B", "C4", "FLY", "NPY", "PERF", "PL", "RUF", "YTT", "INP", "ERA", "TID", "TCH", "N", "UP", "PT", "G", "T20", "COM"]
ignore = [
    "PLR0913",  # Too many arguments in a function
    "PLR2004",  # Magic value used in comparison
    "PLR0911",  # Too many return statements in a function
    "PLR0912",  # Too many branches in a function
    "PLR0915",  # Too many statements in a function
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"]
"spruce/experiments/**/*.py" = ["T201", "G004"]

[tool.ruff.lint.isort]
force-single-line = true
known-first-party = ["spruce"]


[tool.pytest.ini_options]
addopts = "--cov=spruce --cov-report=term-missing"

[tool.coverage.run]
source = ["spruce"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]

[tool.ty]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
